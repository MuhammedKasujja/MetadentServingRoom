import 'package:flutter/material.dart';
import 'package:metadent_serving_app/infra/infra.dart';

class InServiceAppointmentItem extends StatelessWidget {
  final Appointment appointment;
  const InServiceAppointmentItem({Key? key, required this.appointment})
      : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
